# Start Here

1. Read `CURRENT_IMPLEMENTATION_STATUS.md`.
2. Use the root backend/frontend folders as the runnable hardened source baseline.
3. Read `docs/latest-ai-ui-design/` and `docs/latest-academic-pack/` for the newest design and university-document work.
4. Follow `REMAINING_PLAN.md` to consolidate the newest AI/UI work into source and finish certification.
5. Never deploy credentials from an old archive. Create new environment files from `.env.example` and rotate any credential that appeared in the original uploaded ZIP.
